# Dependencies
node_modules/

# Build output
dist/
dev-dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# System files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Framework / tooling
.tanstack/
.vercel/

# Editors
.vscode/
.idea/

# Scratch files
test_*.ts
test_*.js

# Service account key for Node-side scripts (scripts/firebase-node.mjs).
# Generate via Firebase Console > Project Settings > Service Accounts > Generate new private key.
service-account.json

# Local backups and migration output (see scripts/backup.mjs, scripts/optimize-covers.mjs).
# backup_images/ holds the pre-compression originals and is the rollback source
# for the cover migration — keep it, just don't commit ~370 MB of it.
backup.json
backup_images/
migration-report.json
.vercel/
